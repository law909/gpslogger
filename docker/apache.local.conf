<VirtualHost *:80>
    ServerAdmin webadmin@localhost
    ServerName gpslogger.local

    DocumentRoot /var/www/html/public

    ErrorLog ${APACHE_LOG_DIR}/gpslogger.local-error.log
    LogLevel warn
    CustomLog ${APACHE_LOG_DIR}/gpslogger.local-access.log combined

    ServerSignature On
    AddDefaultCharset utf-8

    <FilesMatch \.php$>
        SetHandler "proxy:unix:/run/php/php8.3-fpm.sock|fcgi://localhost/"
    </FilesMatch>

    <IfModule mod_dir.c>
	    DirectoryIndex index.php
    </IfModule>

    <Directory /var/www/html/public>
        Options -Indexes +FollowSymLinks +MultiViews
        AllowOverride All
        Order allow,deny
        allow from all
    </Directory>
</VirtualHost>
